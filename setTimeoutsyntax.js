//syntax
//these method executes a function,after waiting a specified number of milliseconds
let timeID = setTimeout(func, delay)
//func:the first parameter is a function to be executed
//delay:indicates the time in millisecond after which the above function is to be called
